{
    "login": {
        "email": {
            "required": "Email is required",
            "invalid": "Invalid email address"
        },
        "password": {
            "required": "Password is required",
            "min": "Password must be at least {{length}} characters long"
        }
    },
    "user": {
        "name": {
            "required": "Name is required"
        },
        "email": {
            "required": "Email is required",
            "invalid": "Invalid email address",
            "exists": "Email already exists"
        },
        "password": {
            "required": "Password is required",
            "min": "Password must be at least {{length}} characters long"
        },
        "status": {
            "required": "Status is required",
            "invalid": "Invalid status"
        }
    },
    "required": "{{field}} is required",
    "unique": "{{field}} already exists",
    "invalid": "{{field}} is invalid",
    "min": "{{field}} must be at least {{length}} characters long",
    "min_value": "{{field}} must be greater than {{min}}",
    "in": "{{field}} must be one of the following: {{values}}",
    "invalid_number" : "{{field}} can't receive string"
}